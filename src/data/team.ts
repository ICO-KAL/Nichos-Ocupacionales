export type TeamMember = {
  name: string;
  studentId: string;
  phone: string;
  telegramUsername: string;
  photo: string;
};

// Add the confirmed contact details of each project member here.
export const teamMembers: TeamMember[] = [];